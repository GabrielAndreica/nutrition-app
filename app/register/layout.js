export const metadata = {
  title: 'Înregistrare',
  description: 'Creează gratuit un cont Trevano.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function RegisterLayout({ children }) {
  return children;
}
